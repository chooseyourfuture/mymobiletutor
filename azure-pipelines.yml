# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
#- task: ArchiveFiles@2
#  inputs:
#    rootFolderOrFile: '$(build.sourcesDirectory)'
#    includeRootFolder: false
#- task: PublishBuildArtifacts@1
- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'ftps://studentmobileapp.com'
    username: 'admin@studentmobileapp.com'
    password: 'kJcik3Uq8y5JMZf'
    rootDirectory: '$(build.sourcesDirectory)'
    filePatterns: '**'
    remoteDirectory: '/public_html/git-test'
    clean: false
    cleanContents: false
    preservePaths: true
    trustSSL: true